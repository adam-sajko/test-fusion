# @test-fusion/playwright-coverage

Istanbul-based coverage collection for Playwright tests. Collects `window.__coverage__` from instrumented apps after each test and merges it into a standard `coverage-final.json`.

For full documentation, see the [main README](https://github.com/adam-sajko/test-fusion#playwright).
